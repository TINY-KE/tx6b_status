page {
  --brand: #185fa5;

  /* 状态配色总表（备查，页面样式里为兼容老基础库仍写死十六进制）。
     改配色时这几处必须同步，否则同一个状态在不同页面颜色会不一致：
       app.wxss(这里) / pages/board/board.wxss(.st- .d- .tag- .bar-) /
       pages/fill/fill.wxss(.type- .rec-tag) / utils/status.js(TYPES 的 color/bg/text)
     在岗=绿 · 京内出差=黄 · 京外出差=红 · 请假=灰
     京内分两个黄：点/条/选中按钮底用亮黄 #ffc107（小色块要看得出是黄色，越亮越好）；
     统计数字/文字用金 #c08a00——亮黄当文字压白底只有 1.6:1 完全读不了，
     #c08a00 是「在白底上还能读」的最亮的黄（3.05:1，统计数字是 38rpx 大字，按 3:1 门槛）。
     请假用 #6f6e68 而不是更浅的 #888780：选中态是白字压灰底，浅灰只有 3.6:1 达不到 4.5:1。 */
  --st-office: #2f8f5b;
  --st-office-bg: #e6f4ec;
  --st-meeting: #c08a00;
  --st-meeting-bg: #fef3c7;
  --st-trip: #c0392b;
  --st-trip-bg: #fbe9e7;
  --st-leave: #6f6e68;
  --st-leave-bg: #f1efe8;

  --text-1: #1c1c1e;
  --text-2: #6b6b70;
  --text-3: #9a9aa0;
  --line: rgba(0, 0, 0, 0.08);
  --surface: #ffffff;
  --page-bg: #f5f5f7;

  background-color: var(--page-bg);
  color: var(--text-1);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Helvetica Neue', sans-serif;
  font-size: 36rpx;
  -webkit-font-smoothing: antialiased;
}

view,
text {
  box-sizing: border-box;
}

.container {
  padding: 24rpx;
}

.card {
  background: var(--surface);
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 24rpx;
}

.card-tight {
  padding: 8rpx 24rpx;
}

.sec-title {
  font-size: 38rpx;
  font-weight: 500;
  color: var(--text-1);
  margin-bottom: 16rpx;
}

.muted {
  color: var(--text-2);
  font-size: 32rpx;
}

.hint-text {
  color: var(--text-3);
  font-size: 32rpx;
  line-height: 1.6;
}

.row-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.empty {
  padding: 80rpx 0;
  text-align: center;
  color: var(--text-3);
  font-size: 34rpx;
}

.btn-primary {
  background: var(--brand);
  color: #ffffff;
  border-radius: 12rpx;
  font-size: 38rpx;
  font-weight: 500;
  line-height: 96rpx;
  text-align: center;
}

.btn-primary.btn-disabled {
  opacity: 0.4;
}

.btn-plain {
  background: var(--surface);
  color: var(--brand);
  border: 1rpx solid var(--brand);
  border-radius: 12rpx;
  font-size: 36rpx;
  line-height: 84rpx;
  text-align: center;
}
